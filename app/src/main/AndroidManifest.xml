<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ale.test_layer" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"

        android:theme="@style/Theme.AppCompat.Light.NoActionBar" >

        <activity
            android:name="presentation_layer.LoginPage"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="presentation_layer.Landing_Page"/>
        <activity android:name="presentation_layer.SignUp_Page"/>
        <activity android:name="presentation_layer.VerificationActivity"/>
        <activity android:name="presentation_layer.Create_Tour"/>
        <activity android:name="presentation_layer.Browse_Tours"/>
    </application>

</manifest>
